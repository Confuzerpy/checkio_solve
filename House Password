import re
def checkio(password):
    if len(password) >= 10 and re.search("[a-z]+", password) and re.search("[A-Z]+", password) and re.search("[0-9]+", password):
        return True
    else:
        return False
